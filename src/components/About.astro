<section id="about">
  <div class="container">
    <div class="reveal">
      <span class="section-label">Sobre mí</span>
      <h2 class="section-title">
        Código con <span class="gradient-text">propósito</span>
      </h2>
    </div>
    <div class="about-grid">
      <div class="about-text reveal" style="transition-delay: 0.1s;">
        <p>
          Soy un desarrollador de software con <strong>más de 5 años de experiencia autodidacta</strong>
          y actualmente cursando <strong>Desarrollo de Aplicaciones Web</strong> para fortalecer
          y formalizar mis conocimientos técnicos.
        </p>
        <p>
          Me especializo en <strong>Java</strong>, <strong>Kotlin</strong> y <strong>JavaScript</strong>,
          construyendo tanto aplicaciones backend robustas como interfaces web modernas.
          Disfruto especialmente enfrentando problemas complejos y convirtiendo los requisitos
          en soluciones limpias, eficientes y mantenibles.
        </p>
        <p>
          Busco activamente incorporarme a un equipo donde pueda aportar valor real desde el primer día,
          seguir creciendo profesionalmente y trabajar en proyectos con impacto.
        </p>

        <div class="values-row">
          <div class="value-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
            Código limpio y mantenible
          </div>
          <div class="value-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
            Aprendizaje continuo
          </div>
          <div class="value-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
            Atención al detalle
          </div>
          <div class="value-item">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
            Trabajo en equipo
          </div>
        </div>

        <div class="stack-block">
          <span class="stack-label">Stack principal</span>
          <div class="stack-tags">
            <span>Java</span><span>Kotlin</span><span>JavaScript</span>
            <span>Python</span><span>HTML/CSS</span><span>Astro</span>
            <span>Node.js</span><span>MySQL</span><span>Git</span>
          </div>
        </div>

        <div class="about-cta">
          <a href="#contact" class="btn btn-primary">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
            Hablemos
          </a>
          <a href="https://www.linkedin.com/in/javier-garrido-terron/" target="_blank" rel="noopener noreferrer" class="btn btn-outline">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
            LinkedIn
          </a>
        </div>
      </div>

      <div class="about-visual reveal" style="transition-delay: 0.2s;">
        <div class="avatar-container">
          <div class="avatar-ring">
            <div class="avatar">
              <span class="avatar-initials gradient-text">JG</span>
            </div>
          </div>
          <div class="avatar-glow"></div>
        </div>
        <div class="stats-grid">
          <div class="stat-card">
            <span class="stat-num gradient-text">5+</span>
            <span class="stat-lbl">Años programando</span>
          </div>
          <div class="stat-card">
            <span class="stat-num gradient-text">10+</span>
            <span class="stat-lbl">Proyectos</span>
          </div>
          <div class="stat-card">
            <span class="stat-num gradient-text">9+</span>
            <span class="stat-lbl">Tecnologías</span>
          </div>
          <div class="stat-card">
            <span class="stat-num gradient-text">4</span>
            <span class="stat-lbl">Certificados</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-grid {
    display: grid;
    grid-template-columns: 1.25fr 1fr;
    gap: 5rem;
    align-items: start;
  }

  .about-text p {
    color: var(--text-secondary);
    margin-bottom: 1.25rem;
    font-size: 1rem;
    line-height: 1.85;
  }

  .about-text strong { color: var(--text); font-weight: 600; }

  /* Values */
  .values-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.65rem;
    margin: 1.5rem 0;
  }

  .value-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
  }

  .value-item svg { color: var(--accent); flex-shrink: 0; }

  /* Stack */
  .stack-block { margin: 1.5rem 0; }

  .stack-label {
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    display: block;
    margin-bottom: 0.65rem;
  }

  .stack-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .stack-tags span {
    font-size: 0.78rem;
    font-weight: 500;
    padding: 0.3rem 0.8rem;
    border-radius: var(--radius-xs);
    background: rgba(56, 189, 248, 0.06);
    color: var(--text-secondary);
    border: 1px solid rgba(56, 189, 248, 0.1);
    transition: all 0.3s;
    font-family: 'JetBrains Mono', monospace;
  }

  .stack-tags span:hover {
    background: rgba(56, 189, 248, 0.12);
    border-color: rgba(56, 189, 248, 0.25);
    color: var(--accent);
    transform: translateY(-1px);
  }

  /* CTA */
  .about-cta { display: flex; gap: 1rem; margin-top: 2rem; flex-wrap: wrap; }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius-sm);
    font-weight: 600;
    font-size: 0.9rem;
    transition: all 0.3s;
    cursor: pointer;
    border: 1px solid transparent;
    font-family: inherit;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    color: #050510;
    font-weight: 700;
    box-shadow: 0 4px 20px rgba(56, 189, 248, 0.2);
  }

  .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 8px 28px rgba(56, 189, 248, 0.3); }

  .btn-outline {
    background: transparent;
    color: var(--text);
    border-color: var(--border);
  }

  .btn-outline:hover {
    border-color: var(--accent);
    color: var(--accent);
    background: rgba(56, 189, 248, 0.06);
    transform: translateY(-3px);
  }

  /* Visual side */
  .about-visual {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
    position: relative;
  }

  .avatar-container {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .avatar-ring {
    width: 200px; height: 200px;
    border-radius: 50%;
    padding: 3px;
    background: conic-gradient(var(--accent), var(--accent2), var(--accent3), var(--accent));
    animation: spin 8s linear infinite;
    position: relative;
    z-index: 1;
  }

  .avatar {
    width: 100%; height: 100%;
    border-radius: 50%;
    background: var(--bg);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .avatar-initials {
    font-size: 3.5rem;
    font-weight: 900;
    letter-spacing: -0.04em;
  }

  .avatar-glow {
    position: absolute;
    width: 220px; height: 220px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(56, 189, 248, 0.14) 0%, transparent 70%);
    filter: blur(20px);
    z-index: 0;
  }

  @keyframes spin { to { transform: rotate(360deg); } }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    width: 100%;
  }

  .stat-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 1.1rem;
    text-align: center;
    transition: all 0.3s;
  }

  .stat-card:hover {
    border-color: var(--border-hover);
    background: var(--bg-card-hover);
    transform: translateY(-3px);
  }

  .stat-num {
    display: block;
    font-size: 1.6rem;
    font-weight: 800;
    letter-spacing: -0.03em;
  }

  .stat-lbl {
    font-size: 0.72rem;
    color: var(--text-muted);
    margin-top: 0.15rem;
    font-weight: 500;
  }

  @media (max-width: 900px) {
    .about-grid { grid-template-columns: 1fr; gap: 3rem; }
    .avatar-ring { width: 160px; height: 160px; }
    .avatar-initials { font-size: 2.8rem; }
    .values-row { grid-template-columns: 1fr; }
  }
</style>
