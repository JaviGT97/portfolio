<section id="sobre-mi">
  <div class="container">
    <div class="section-header reveal">
      <span class="section-label">Sobre mí</span>
      <h2 class="section-title">Autodidacta hasta la médula. <span class="accent-text">Profesional por vocación.</span></h2>
      <p class="section-desc">Desde mi primera línea de código en 2020 hasta hoy: aprendiendo a mi ritmo, construyendo proyectos reales y sin planes de parar.</p>
    </div>

    <div class="about-grid">
      <div class="about-content reveal">
        <div class="content-block">
          <h3>Mi historia</h3>
          <p>
            Todo empezó en 2020 con un <strong>video en la escuela</strong> que me despertó la curiosidad.
            Me puse a hacer un curso de programación sin saber muy bien qué esperar, y para la primera clase
            ya estaba enganchado. Lo que empezó como una cosa "a ver qué tal" se convirtió en algo
            que hacía todos los días sin que nadie me obligara.
          </p>
          <p>
            Desde entonces <strong>no he parado</strong>. Me gusta aprender cosas nuevas, resolver problemas
            que al principio parecen imposibles y convertirlos en algo que funciona y tiene buena pinta.
            Código que se entiende, interfaces que no dan dolor de cabeza y arquitecturas que no se caen
            cuando crece el proyecto.
          </p>
        </div>

        <div class="content-block">
          <h3>Lo que uso día a día</h3>
          <div class="tech-stack-grid">
            <div class="tech-mini-card" style="--cat-color: #00d4aa">
              <div class="tech-mini-bar"></div>
              <h4>Lenguajes</h4>
              <div class="tech-chips">
                <span class="tech-chip"><span class="chip-dot" style="background: #e76f00"></span>Java</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #7f52ff"></span>Kotlin</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #f7df1e"></span>JavaScript</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #3776ab"></span>Python</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #e34c26"></span>HTML/CSS</span>
              </div>
            </div>
            <div class="tech-mini-card" style="--cat-color: #38bdf8">
              <div class="tech-mini-bar"></div>
              <h4>Herramientas</h4>
              <div class="tech-chips">
                <span class="tech-chip"><span class="chip-dot" style="background: #f05032"></span>Git</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #f24e1e"></span>Figma</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #2496ed"></span>Docker</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #2088ff"></span>Vercel</span>
                <span class="tech-chip"><span class="chip-dot" style="background: #2088ff"></span>GitHub Actions</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="about-visual reveal" style="transition-delay: 0.2s;">
        <div class="avatar-container">
          <div class="avatar-ring">
            <div class="avatar">
              <span>GT</span>
            </div>
          </div>
          <div class="avatar-glow avatar-glow-1"></div>
          <div class="avatar-glow avatar-glow-2"></div>
        </div>

        <div class="experience-stats">
          <div class="stat-card">
            <div class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2"/><rect x="9" y="3" width="6" height="4" rx="1"/><path d="M9 14l2 2 4-4"/></svg>
            </div>
            <div class="stat-info">
              <span class="stat-value" data-count="7">0<span>+</span></span>
              <span class="stat-label">Proyectos entregados</span>
            </div>
          </div>

          <div class="stat-card">
            <div class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
            </div>
            <div class="stat-info">
              <span class="stat-value" data-count="6">0<span>+</span></span>
              <span class="stat-label">Certificaciones</span>
            </div>
          </div>

          <div class="stat-card">
            <div class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
            </div>
            <div class="stat-info">
              <span class="stat-value">2020</span>
              <span class="stat-label">Empecé en el desarrollo</span>
            </div>
          </div>

          <div class="stat-card">
            <div class="stat-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>
            </div>
            <div class="stat-info">
              <span class="stat-value" data-count="3">0<span>+</span></span>
              <span class="stat-label">Tecnologías principales</span>
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<script>
  // Animated counter
  const counters = document.querySelectorAll('.stat-value[data-count]');
  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const el = entry.target as HTMLElement;
        const target = parseInt(el.dataset.count || '0');
        let current = 0;
        const duration = 1200;
        const step = target / (duration / 16);
        const timer = setInterval(() => {
          current += step;
          if (current >= target) {
            current = target;
            clearInterval(timer);
          }
          el.innerHTML = `${Math.floor(current)}<span>+</span>`;
        }, 16);
        counterObserver.unobserve(el);
      }
    });
  }, { threshold: 0.5 });

  counters.forEach(c => counterObserver.observe(c));
</script>

<style>
  #sobre-mi {
    padding: 3.5rem 0;
    position: relative;
  }

  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 5rem;
  }

  .section-title {
    font-size: clamp(2.2rem, 4.5vw, 3.2rem);
    font-weight: 700;
    line-height: 1.3;
    margin-top: var(--space-5);
  }

  .accent-text {
    color: var(--accent-primary);
    position: relative;
  }

  .accent-text::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--accent-primary), transparent);
    border-radius: 2px;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1.8fr 1.2fr;
    gap: var(--space-12);
    margin-bottom: var(--space-16);
  }

  @media (max-width: 1024px) {
    .about-grid { grid-template-columns: 1fr; gap: var(--space-10); }
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-10);
  }

  .content-block {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .content-block h3 {
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--fg-primary);
    margin-bottom: var(--space-2);
  }

  .content-block p {
    color: var(--fg-secondary);
    line-height: 1.8;
    font-size: 1.05rem;
  }

  .content-block strong {
    color: var(--accent-primary);
    font-weight: 600;
  }

  .tech-stack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-4);
    margin-top: var(--space-4);
  }

  .tech-mini-card {
    position: relative;
    background: var(--bg-card);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-md);
    padding: 1.1rem 1.2rem;
    overflow: hidden;
    transition: all var(--transition-base);
  }

  .tech-mini-card:hover {
    border-color: color-mix(in srgb, var(--cat-color) 25%, transparent);
    transform: translateY(-3px);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.15);
  }

  .tech-mini-bar {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--cat-color);
    opacity: 0.6;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--transition-slow);
  }

  .tech-mini-card:hover .tech-mini-bar {
    transform: scaleX(1);
    opacity: 1;
  }

  .tech-mini-card h4 {
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--cat-color);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-3);
  }

  .tech-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .tech-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.4rem 0.75rem;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-full);
    font-size: 0.82rem;
    font-weight: 500;
    color: var(--fg-secondary);
    transition: all var(--transition-fast);
  }

  .tech-chip:hover {
    border-color: color-mix(in srgb, var(--cat-color) 30%, transparent);
    color: var(--fg-primary);
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-1px);
  }

  .chip-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  /* Visual side */
  .about-visual {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
    align-items: center;
  }

  .avatar-container {
    position: relative;
    width: 220px;
    height: 220px;
    margin: 0 auto;
  }

  .avatar-ring {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    padding: 3px;
    background: conic-gradient(from 0deg, var(--accent-primary), var(--accent-secondary), var(--accent-tertiary), var(--accent-primary));
    animation: rotateRing 8s linear infinite;
    position: relative;
    z-index: 1;
  }

  .avatar {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: var(--bg-base);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    font-weight: 700;
    color: var(--fg-primary);
    font-family: var(--font-display);
  }

  .avatar-glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(50px);
    opacity: 0.5;
    animation: glowPulse 4s ease-in-out infinite;
    z-index: 0;
  }

  .avatar-glow-1 {
    width: 120px; height: 120px;
    background: radial-gradient(circle, var(--accent-primary), transparent);
    top: -30px; right: -30px;
  }

  .avatar-glow-2 {
    width: 80px; height: 80px;
    background: radial-gradient(circle, var(--accent-secondary), transparent);
    bottom: -20px; left: -20px;
    animation-delay: 1.5s;
  }

  @keyframes rotateRing { from { transform: rotate(360deg); } to { transform: rotate(0deg); } }
  @keyframes glowPulse { 0%, 100% { opacity: 0.5; transform: scale(1); } 50% { opacity: 0.3; transform: scale(0.9); } }

  .experience-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
    width: 100%;
  }

  .stat-card {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4);
    background: var(--bg-card);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-md);
    transition: all var(--transition-base);
    backdrop-filter: blur(12px);
  }

  .stat-card:hover {
    transform: translateY(-4px) translateX(4px);
    border-color: var(--border-accent);
    box-shadow: 0 12px 36px rgba(0, 0, 0, 0.2);
  }

  .stat-icon {
    width: 44px; height: 44px;
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 212, 170, 0.08);
    color: var(--accent-primary);
    flex-shrink: 0;
  }

  .stat-info { flex: 1; }

  .stat-value {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--fg-primary);
    line-height: 1;
    margin-bottom: var(--space-1);
    font-family: var(--font-display);
  }

  .stat-value span { color: var(--accent-primary); font-weight: 600; }

  .stat-label {
    font-size: 0.82rem;
    color: var(--fg-secondary);
  }

  @media (max-width: 768px) {
    .experience-stats { grid-template-columns: 1fr; }
    .avatar-container { width: 180px; height: 180px; }
    .avatar { font-size: 2.5rem; }
  }

  @media (prefers-reduced-motion: reduce) {
    .avatar-ring, .avatar-glow { animation: none; }
  }
</style>
